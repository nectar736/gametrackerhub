{
  "name": "board-game-tracker",
  "version": "1.0.0",
  "description": "A static board game tracking application with localStorage persistence",
  "main": "index.js",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "dev": "eleventy --serve --port 3000"
  },
  "keywords": ["board-games", "tracker", "eleventy", "static-site"],
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
